@using FreyaMusicStore
@{
    Layout = "mylayout";
}

@section Main
{
<h3>Browse Genres</h3>

<p>
    Select from @Model.Genres.Count() genres:
</p>
<ul>
    @foreach (var genre in Model.Genres)
    {
    <li><a href="@String.Format(@Uris.genre, genre.Name)">@genre.Name</a></li>
    }
</ul>
}